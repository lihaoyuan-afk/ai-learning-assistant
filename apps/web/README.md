# Web App

Next.js frontend for the AI Learning Assistant Agent.

## Run

```powershell
cd E:\codex\apps\web
npm install
npm run dev
```

The default API URL is `http://localhost:8000`. Override it with `NEXT_PUBLIC_API_BASE_URL`.

